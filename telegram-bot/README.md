# 🤖 EUBike Telegram Bot

Профессиональный бот для автоматизации наполнения каталога велосипедов EUBike. Бот парсит объявления с **Kleinanzeigen.de**, обрабатывает их с помощью **Искусственного Интеллекта (Google Gemini 2.0 + Groq Llama 3)** и управляет базой данных товаров.

---

## 🚀 Основные возможности

*   **Мультимодальный анализ**: Использует зрение (Vision) для анализа фотографий велосипедов и текст объявления для извлечения характеристик.
*   **Умная очередь**: Последовательная обработка ссылок для обхода защиты от ботов и лимитов API.
*   **Авто-коррекция**: Массовая проверка и исправление категорий/дисциплин (например, отличие *MTB Enduro* от *MTB Trail*).
*   **Мониторинг цен**: Отслеживание изменений цены и статуса (резерв/продано) для уже добавленных велосипедов.
*   **Failover архитектура**: Автоматическое переключение на резервную нейросеть (Groq), если основная (Gemini) недоступна.

---

## 🛠 Архитектура проекта

Бот построен на **Node.js** и использует гибридный подход к парсингу (HTML-запросы + Headless Browser).

### Ключевые компоненты:

1.  **`bot.js`** — Точка входа. Управляет маршрутизацией команд, очередями сообщений и взаимодействием с пользователем.
2.  **`kleinanzeigen-parser.js`** — Модуль парсинга. Использует **Puppeteer** (с плагином stealth) для обхода защиты Cloudflare/Datadome. Делает скриншоты страниц для визуального анализа.
3.  **`gemini-processor.js` / `geminiClient.ts`** — Интеграция с **Google Gemini**.
    *   Управляет пулом API-ключей (Rotation).
    *   Контролирует Rate Limits (RPM/TPM).
    *   Выполняет визуальный анализ геометрии рамы и компонентов.
4.  **`groq-integration.js`** — Интеграция с **Groq (Llama 3.3)**. Используется как сверхбыстрый текстовый анализатор и резервный канал при сбоях Gemini.
5.  **`bikes-database-node.js`** — ORM для работы с **SQLite**. Управляет таблицами `bikes`, `analytics`, `subscribers`.
6.  **`image-handler.js`** — Скачивание, оптимизация (WebP) и хранение изображений.

---

## 🎮 Команды бота

### 🟢 Основные команды

| Команда | Описание |
| :--- | :--- |
| `/start` | Запуск бота, приветствие. |
| `/help` | Вывод справки по командам. |
| `/stop` | **Экстренная остановка**. Очищает очереди обработки и прерывает текущие циклы. Полезно при ошибках. |
| `/queue` | Показать статус очереди (сколько ссылок в обработке и ожидании). |
| `/stats` | Показать статистику (аптайм, использование памяти, статус драйверов). |

### 🔍 Парсинг и Анализ

| Команда | Аргументы | Описание |
| :--- | :--- | :--- |
| **(Просто ссылка)** | `https://...` | Автоматически ставит ссылку в очередь. Создает карточку товара, если её нет. |
| `/test` | `<ссылка>` | **Тестовый прогон**. Выполняет полный цикл анализа (парсинг -> AI -> пост-обработка), но **НЕ сохраняет** в БД. Выводит подробный JSON-лог и превью. |
| `/tester` | `<ссылка>` | Запуск через экспериментальный "HTML-first" пайплайн (быстрее, чем Puppeteer). |
| `/groq_card` | `<ссылка>` | Принудительное создание карточки, используя только мощности Groq (текстовый анализ). |
| `/gemini` | `<ссылка>` | Принудительная обработка через Gemini (ручной режим). |

### 🛠 Обслуживание базы данных

| Команда | Аргументы | Описание |
| :--- | :--- | :--- |
| `/check` | `<число>` | Проверить **N** самых давно не обновлявшихся велосипедов. Обновляет цены, ставит статус "Продано" или "Резерв". |
| `/check` | `<ссылка>` | Проверить конкретное объявление на актуальность. |
| `/discheck` | `<число>` | **Discipline Check**. Берет N велосипедов и проверяет правильность их дисциплины (например, уточняет *Gravel* -> *Gravel Race*). <br>ℹ️ *Имеет встроенный механизм Fallback: если Gemini падает, переключается на Groq.* |
| `/cleanup` | — | Удаление старых/лишних изображений с диска. |
| `/cleanall` | — | Интерактивное меню для **удаления велосипедов** из базы (только бот или все). Требует подтверждения. |

### ⚙️ Прочее

| Команда | Описание |
| :--- | :--- |
| `/rate` | Обновить курс валют (EUR/RUB) с сайта банка. |
| `/llmtest` | Стресс-тест API ключей Gemini (отправляет 5 одновременных запросов). |

---

## 🧩 Логика работы (Pipeline)

1.  **Получение ссылки**: Пользователь отправляет ссылку. Бот валидирует её (regex) и добавляет в очередь.
2.  **Скрапинг (Scraping)**:
    *   Запускается Headless Browser.
    *   Проверяются куки и согласие GDPR.
    *   Делаются скриншоты (верхняя часть + спецификации).
    *   Скачивается HTML.
3.  **AI Анализ (The Brain)**:
    *   Скриншоты и текст отправляются в Gemini Pro Vision.
    *   Промпт просит извлечь: Бренд, Модель, Год, Состояние, Размер рамы, Групсет, Вилку и т.д.
    *   Если Gemini недоступен, текстовая версия отправляется в Groq.
4.  **Пост-обработка**:
    *   Очистка цен от мусора.
    *   Валидация категорий по белому списку.
    *   Расчет скидки (если указана оригинальная цена).
5.  **Сохранение**:
    *   Запись в SQLite.
    *   Скачивание фото в локальную папку `public/images/bikes/...`.
    *   Отправка уведомления пользователю с превью карточки.

---

## 📂 Структура файлов

```text
telegram-bot/
├── bot.js                  # 🧠 Главный мозг. Логика команд и оркестрация.
├── bikes-database-node.js  # 💾 Слой данных (SQLite).
├── kleinanzeigen-parser.js # 🕷️ Скрапер (Puppeteer).
├── gemini-processor.js     # 🤖 Обертка над Gemini API.
├── groq-integration.js     # ⚡ Обертка над Groq API.
├── image-handler.js        # 🖼️ Скачивание и ресайз картинок.
├── status-checker.js       # 🕵️ Проверка статуса (жив ли листинг).
├── post-processor.js       # 🧹 Чистка данных перед записью в БД.
├── autocat-klein/          # 📦 Подмодуль с расширенной логикой (TypeScript).
│   └── src/lib/geminiClient.ts # Продвинутый клиент Gemini с ротацией ключей.
└── screenshots/            # 📸 Временная папка для скриншотов парсера.
```

## 🔧 Установка и запуск

1.  **Зависимости**:
    ```bash
    npm install
    ```
2.  **Настройка окружения** (`.env` в корне проекта):
    ```env
    BOT_TOKEN=ваш_токен
    GEMINI_API_KEY=ваш_ключ
    GROQ_API_KEY=ваш_ключ_groq
    ```
3.  **Запуск**:
    ```bash
    npm start
    # или для разработки с авто-перезагрузкой:
    npm run dev
    ```
