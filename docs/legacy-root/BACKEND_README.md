# ⚙️ EUBike Backend

Серверная часть платформы EUBike. Представляет собой REST API на Node.js, обеспечивающее работу фронтенда, Telegram-ботов и интеграцию с внешними сервисами.

---

## 🛠 Технологический стек

*   **Runtime:** Node.js.
*   **Framework:** Express.js.
*   **База данных:** SQLite (хранение данных в файле `eubike.db`).
    *   *Примечание: В коде могут встречаться названия файлов типа `mysql-config.js` — это легаси-названия, фактически используется SQLite.*
*   **AI & ML:** Google Gemini API (генерация описаний), Google Translate API (перевод контента).
*   **Безопасность:** JWT (JSON Web Tokens) для авторизации, bcrypt для хеширования паролей.
*   **Почта:** Nodemailer (SMTP) для отправки уведомлений и сброса паролей.

---

## 📂 Структура проекта

```text
backend/
├── API/
│   └── server.js           # 🚀 Точка входа. Настройка Express, Middleware и роутов.
├── database/               # 💾 Файлы базы данных и схемы
│   ├── eubike.db           # Основной файл БД SQLite
│   └── schema.sql          # SQL схема таблиц
├── public/
│   └── images/             # 🖼️ Статические файлы (фото велосипедов)
├── scripts/                # 🛠 Скрипты для обслуживания и миграций
│   ├── crm-api.js          # Интеграция с CRM системой
│   ├── migrate-data.js     # Миграции БД
│   └── verify_hot_offer.js # Проверочные скрипты
├── src/
│   └── apis/
│       └── js/
│           └── mysql-config.js # 🔌 Слой доступа к данным (DatabaseManager)
```

---

## 🔌 API Эндпоинты

### 🚲 Велосипеды (`/api/bikes`)
*   `GET /api/bikes` — Получение списка с фильтрацией (search, sort, filters).
*   `GET /api/bikes/:id` — Детальная информация о велосипеде.
*   `GET /api/bikes/filters` — Доступные опции фильтров (бренды, дисциплины).

### 🛒 Заказы (`/api/orders`)
*   `POST /api/orders` — Создание нового заказа.
*   `GET /api/orders/:id` — Статус заказа.
*   `POST /api/orders/guest` — Быстрый заказ без регистрации.

### 👤 Пользователи (`/api/auth`)
*   `POST /api/auth/register` — Регистрация.
*   `POST /api/auth/login` — Вход (выдача JWT).
*   `POST /api/auth/refresh` — Обновление токена.

### 🤖 Интеграции
*   **Telegram Bot Webhook:** Эндпоинты для взаимодействия с ботом.
*   **CRM Sync:** Синхронизация статусов заказов и лидов.

---

## 💾 База данных

Используется реляционная структура в SQLite. Основные таблицы:
*   `bikes` — Каталог товаров (характеристики, цены, фото).
*   `users` — Клиенты и администраторы.
*   `orders` — История заказов.
*   `analytics` — Данные для аналитики просмотров и продаж.

**Особенность ранжирования:**
В системе реализован алгоритм ранжирования товаров (`rank`), учитывающий:
1.  Флаг "Горячее предложение" (`is_hot_offer`) — дает приоритет +30.
2.  Новизну добавления.
3.  Наличие скидки.

---

## 🔧 Установка и Запуск

1.  **Установка зависимостей:**
    ```bash
    cd backend
    npm install
    ```

2.  **Настройка окружения (`.env`):**
    ```env
    PORT=8081
    JWT_SECRET=ваш_секрет
    GEMINI_API_KEY=ключ_google_ai
    # Пути к изображениям (опционально)
    IMAGES_DIR=./public/images/bikes
    ```

3.  **Запуск сервера:**
    ```bash
    npm start
    # или для разработки
    npm run dev-api
    ```
    Сервер запустится на порту `8081`.

---

## ⚠️ Важные заметки

*   **Изображения:** Сервер раздает статику из папки `public/images`. Убедитесь, что папка существует и доступна для записи, если планируете загружать новые фото.
*   **Поиск:** Для поиска используется оператор `LIKE`, так как SQLite имеет ограничения по полнотекстовому поиску в стандартной конфигурации.
