# Генеральный Технический Аудит Системы Hunter

**Дата аудита:** 21.01.2026  
**Цель:** Глубокий анализ логики фильтрации, хранения данных, распределения квот и генерации URL.

---

## 1. 🚨 Критическая уязвимость: Фильтрация "Не-велосипедов" (Mopeds/Motorcycles)

### Статус: 🔴 FAILED
**Проблема:** Система пропускает мопеды (Simson, Roller) и мотоциклы, несмотря на наличие `KillSwitchFilter`.

**Анализ кода (`KillSwitchFilter.js`):**
- **Логика:** Используется простой поиск подстрок (`includes`) по списку `blocked` слов: `['motorrad', 'moped', 'roller', 'scooter', ...]`.
- **Почему не работает:**
  1. **Нечеткое совпадение:** Проверка `text.includes(word)` может давать False Negative, если слово написано с опечаткой или является частью другого слова (хотя `\b` границы слов используются в `RegExp`, но список может быть неполным).
  2. **Отсутствие проверки категории Kleinanzeigen:** Парсер игнорирует хлебные крошки (Breadcrumbs) или ID категории самого объявления. Мопеды часто лежат в категории `Motorräder & Motorroller` (ID `305`), а не `Fahrräder` (ID `210`).
  3. **AI Validator (Gemini):** В промпте Gemini (`gemini-processor.js`) инструкция `isBike` может быть недостаточно строгой для отличия электро-скутера от e-bike.

**Решение:**
- Внедрить **проверку ID категории** на этапе парсинга (`kleinanzeigen-parser.js`). Если категория != `Fahrräder` (ID ~217), немедленно отбрасывать.
- Добавить список "Токсичных брендов" (Simson, Vespa, Yamaha) в `KillSwitch`.

---

## 2. 🌊 Озеро Данных (Data Lake & Market History)

### Статус: 🟡 PARTIAL
**Проблема:** "Тихий сборщик" сохраняет данные, но фильтрация перед сохранением слабая.

**Анализ кода (`unified-hunter.js` -> `fetchMarketData`):**
- **Логика:** 
  1. Скачивается страница поиска.
  2. Прогоняется через `TechDecoder.decode` (статическая проверка).
  3. Если `decoded.isBike === true`, сохраняется в `market_history`.
- **Риск:** `TechDecoder` — это RegEx-эвристика. Она может пропустить раму ("Rahmen") как велосипед, если в заголовке нет явного стоп-слова.
- **Инсайт:** В `market_history` попадают лоты, которые *прошли* первичный фильтр, но *не обязательно* прошли глубокий AI-анализ. Это "грязное" озеро.

**Решение:**
- Добавить поле `is_verified` в `market_history`.
- Периодически запускать "Очиститель Озера" (Lake Cleaner), который прогоняет старые записи через Gemini Flash (дешево) для уточнения статуса.

---

## 3. 📊 Распределение Категорий (Quotas)

### Статус: 🟢 PASSED (Logic Exists)
**Проблема:** Как обеспечивается 45% MTB / 25% Gravel?

**Анализ кода (`test-autocat.js` -> `runAutonomous`):**
- **Логика:** Жесткое кодирование пропорций:
  ```javascript
  const mtbCount = Math.round(count * 0.60); // 60% (немного отличается от ТЗ 45%)
  const emtbCount = Math.round(count * 0.20);
  const roadGravelCount = count - mtbCount - emtbCount;
  ```
- **Реализация:** Функция последовательно вызывает `processCategoryQuota` для каждой категории.
- **Нюанс:** В `unified-hunter.js` (`hunt` method) также есть логика `DiversityManager`, но она работает на уровне выбора целей (`getSmartTargets`), а не жестких квот.

**Решение:**
- Синхронизировать пропорции в `test-autocat.js` с требованиями (45/25/20/8/2).
- Внедрить глобальный `SessionState`, который помнит, сколько уже найдено, чтобы при перезапуске не начинать с нуля (MTB).

---

## 4. 🔗 Генерация Search URL

### Статус: 🟡 NEEDS IMPROVEMENT
**Проблема:** Используются ли ID категорий для точности?

**Анализ кода (`unified-hunter.js` -> `constructUrl`):**
- **Логика:**
  - Базовый URL: `https://www.kleinanzeigen.de/s-fahrraeder`
  - Суффикс бренда: `k0c217` (где `c217` — это категория Fahrräder).
  - Тип (Type): Добавляется через `+fahrraeder.type_s:mountainbike` (для MTB) или `rennrad` (для Road).
- **Уязвимость:**
  - Для **Gravel** тип передается как `null`, так как у Kleinanzeigen нет отдельного фильтра "Gravel". Поиск идет только по ключевым словам + категория "Велосипеды". Это и есть причина попадания мусора (поиск по слову "Gravel" в категории "Велосипеды" может выдать шоссейник с покрышками gravel).
  - Для **eMTB** используется `pedelec`, что корректно.

**Решение:**
- Для Gravel: Использовать комбинированный фильтр `c217` + ключевые слова в `UnifiedHunter` (уже есть, но можно усилить минус-словами).
- Проверить, не используется ли случайно `k0` без `c217` в некоторых шаблонах, что расширяет поиск на весь сайт.

---

## Итоговый вердикт

| Компонент | Оценка | Приоритет правки |
| :--- | :---: | :--- |
| **Moped Filter** | 🔴 Критично | **High** (Добавить проверку Breadcrumbs/CatID) |
| **Data Lake** | 🟡 Норма | **Medium** (Внедрить Lake Cleaner) |
| **Quotas** | 🟢 Ок | **Low** (Скорректировать % в конфиге) |
| **URL Gen** | 🟡 Норма | **Medium** (Усилить Gravel-фильтры) |

**Рекомендация:** Начать с "Убийства мопедов" через проверку `breadcrumbs` в парсере.
