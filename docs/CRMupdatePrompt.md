═══════════════════════════════════════════════════════════════════
  CRM SPRINT 3: ТРАНСФОРМАЦИЯ ИЗ "READ-ONLY" В РАБОЧИЙ ИНСТРУМЕНТ
  Цель: Оценка 3/10 → 9-10/10
  Статус: КРИТИЧНАЯ ДОРАБОТКА
  Время: 150-250 минут работы
═══════════════════════════════════════════════════════════════════

## 📋 ОБЯЗАТЕЛЬНОЕ ЧТЕНИЕ ПЕРЕД СТАРТОМ

**ВАЖНО!** Перед выполнением задач прочитай файл:
C:\Users\hacke\CascadeProjects\Finals1\eubike\docs\CRM_SPRINT2_REPORT copy.md



Этот файл содержит:
- Полный отчет тестирования CRM от реального менеджера Алексея
- Детальную оценку каждого модуля (оценка 3/10)
- Список всех критичных проблем и багов
- Контекст текущей реализации
- Что работает, а что нет

**Используй этот отчет как основу для понимания:**
- Что именно не работает
- Что ожидает менеджер от системы
- Какие боли испытывает пользователь
- Что блокирует реальную работу

---

## 🎯 МИССИЯ SPRINT 3

Превратить CRM из "красивого дашборда для наблюдения" в **полноценный инструмент работы**, 
где менеджер может выполнять ВСЮ свою работу без Excel, Telegram, бумажных блокнотов.

**Принцип работы:**
Менеджер должен утром открыть CRM и не закрывать её до конца дня. 
Всё что ему нужно — внутри системы.

---

## 🔥 ТОП-10 КРИТИЧНЫХ ПРОБЛЕМ (ИЗ АУДИТА)

Эти проблемы ОБЯЗАТЕЛЬНО должны быть решены. Приоритет выполнения — СТРОГО по порядку.

### ПРОБЛЕМА 1: НЕАКТИВНЫЙ ЧЕКЛИСТ ПРОВЕРКИ [БЛОКЕР]
### ПРОБЛЕМА 2: КАНБАН БЕЗ DRAG & DROP [КРИТИЧНО]
### ПРОБЛЕМА 3: НЕТ КНОПОК СВЯЗИ С КЛИЕНТОМ [UX]
### ПРОБЛЕМА 4: ЛОГИСТИКА ТОЛЬКО ДЛЯ ЧТЕНИЯ [ФУНКЦИОНАЛ]
### ПРОБЛЕМА 5: ФИНАНСЫ ТОЛЬКО ДЛЯ ЧТЕНИЯ [ФУНКЦИОНАЛ]
### ПРОБЛЕМА 6: НЕТ ЭКСПОРТА В EXCEL [ФУНКЦИОНАЛ]
### ПРОБЛЕМА 7: НЕТ МАССОВЫХ ДЕЙСТВИЙ [UX]
### ПРОБЛЕМА 8: СБРОС ФИЛЬТРОВ ПРИ ПЕРЕЗАГРУЗКЕ [UX]
### ПРОБЛЕМА 9: НЕЛЬЗЯ СОЗДАТЬ ЗАДАЧУ ИЗ ЗАКАЗА [UX]
### ПРОБЛЕМА 10: МЕЛКИЕ КЛИКАБЕЛЬНЫЕ ЗОНЫ [UX]

---

# 🔴 БЛОК 1: ИНТЕРАКТИВНЫЙ ЧЕКЛИСТ ПРОВЕРКИ ВЕЛОСИПЕДА (28 ПУНКТОВ)

## Контекст проблемы

**Что сейчас:** Чеклист отображается как статичный список. Менеджер видит названия 28 пунктов 
(Brand verified, Model verified, Frame condition и т.д.) и какие-то статусы "OK/Issue", 
но это просто текст. Нельзя кликнуть. Нельзя отредактировать. Нельзя добавить комментарий. 
Нельзя загрузить фото дефекта.

**Оценка менеджера:** "Это БЛОКЕР. Это суть моей работы — проверять байки. Сейчас это просто картинка. 
Мне приходится вести чеклист в Google Docs."

**Почему критично:** Проверка велосипеда — это 70% работы менеджера. Без этого функционала 
CRM бесполезна для основной задачи.

---

## Задача 1.1: Backend API для чеклиста

### Endpoint 1: Получение чеклиста заказа

**Метод:** GET  
**Путь:** /api/v1/crm/orders/:orderId/inspection

**Что должен возвращать:**
Полный чеклист из 28 пунктов с текущим состоянием каждого пункта.

**Структура ответа:**
- Список всех 28 пунктов проверки (даже если они ещё не заполнены)
- Для каждого пункта:
  - Текущий статус (проверено / в процессе / проблема / не проверено)
  - Комментарий менеджера (может быть пустым)
  - Список URL фотографий (может быть пустым)
  - Кто проверял (email менеджера)
  - Когда проверял (timestamp)
- Прогресс проверки: сколько пунктов уже проверено из 28
- Процент завершенности

**28 пунктов чеклиста (из JSON схемы):**
1. Бренд подтвержден
2. Модель подтверждена
3. Год подтвержден
4. Размер рамы подтвержден
5. Серийный номер проверен
6. Состояние рамы
7. Состояние вилки
8. Состояние амортизатора
9. Состояние трансмиссии
10. Состояние тормозов
11. Состояние колес
12. Состояние покрышек
13. Проверка рулевой
14. Проверка каретки
15. История обслуживания подвески
16. Износ тормозных колодок (%)
17. Износ цепи
18. Износ кассеты
19. Состояние роторов
20. Люфт в подшипниках
21. Первый владелец
22. Документы о покупке
23. Статус гарантии
24. История падений/аварий
25. Причина продажи
26. Апгрейды подтверждены
27. Тест-драйв пройден
28. Финальное одобрение

**Где хранятся данные:**
В таблице `bikes` или `orders` в колонке `bike_snapshot` (JSONB) внутри объекта `inspection_data.checklist`.

**Логика:**
- Проверить что заказ существует
- Извлечь bike_snapshot из БД
- Найти внутри inspection_data.checklist
- Если чеклиста нет — инициализировать все 28 пунктов пустыми значениями
- Посчитать сколько пунктов имеют status="checked"
- Вернуть JSON

**Если данных нет в БД:**
Вернуть все 28 пунктов с status=null, comment=null, photos=[], checked_at=null, checked_by=null

---

### Endpoint 2: Обновление одного пункта чеклиста

**Метод:** PATCH  
**Путь:** /api/v1/crm/orders/:orderId/inspection/checklist

**Что принимает в теле запроса:**
- Ключ пункта (например "6_frame_condition")
- Новый статус ("checked" | "pending" | "problem")
- Комментарий менеджера (текст)
- Список URL фотографий

**Что должен делать:**
1. Найти заказ в БД по orderId
2. Извлечь bike_snapshot (JSONB)
3. Обновить конкретный пункт чеклиста внутри inspection_data.checklist[item]
4. Записать туда:
   - Новый статус
   - Комментарий
   - Фотографии
   - Время проверки (текущее время)
   - Email менеджера из JWT токена
5. Пересчитать прогресс (сколько пунктов теперь "checked")
6. Обновить bike_snapshot в БД
7. Создать событие в timeline (order_status_events): "Менеджер Иван обновил чеклист: пункт Frame condition → проверено"
8. Логировать действие в audit_log для истории изменений
9. Вернуть успех и новый прогресс

**Важно:**
- Валидация: статус должен быть только "checked", "pending" или "problem"
- Если пункта ещё не было в чеклисте — создать его
- Сохранять кто и когда изменил (для аудита)

---

### Endpoint 3: Загрузка фотографии дефекта

**Метод:** POST  
**Путь:** /api/v1/crm/orders/:orderId/inspection/upload

**Что принимает:**
multipart/form-data с файлом изображения

**Что должен делать:**
1. Принять файл (только изображения: jpg, png, webp)
2. Проверить размер (не больше 5MB)
3. Сохранить файл в папку: /backend/uploads/inspections/:orderId/
4. Имя файла: timestamp-random.jpg (чтобы не было конфликтов)
5. Вернуть полный URL файла: http://localhost:8082/uploads/inspections/:orderId/filename.jpg
6. Этот URL потом используется в PATCH запросе для обновления пункта чеклиста

**Важно:**
- Создать папку uploads/inspections/:orderId/ если её нет
- Настроить статическую раздачу файлов из /uploads через Express
- Ограничить размер файла
- Валидировать что это изображение

---

### Endpoint 4: AI повторная оценка качества

**Метод:** POST  
**Путь:** /api/v1/crm/orders/:orderId/inspection/ai-regrade

**Что принимает:**
Пустое тело запроса (все данные берутся из чеклиста)

**Что должен делать:**
1. Получить заказ из БД
2. Извлечь весь заполненный чеклист
3. Собрать все комментарии менеджера в одну строку
4. Собрать все URL фотографий
5. Сформировать промпт для Gemini AI:
Проведена детальная проверка велосипеда [название байка].

Результаты проверки менеджером:

Рама: [комментарий]

Вилка: [комментарий]

Тормоза: [комментарий]
... (все пункты с комментариями)

Фотографии дефектов: [список URL]

На основе этой информации оцени:

Общее состояние велосипеда по шкале от 1 до 100

Класс качества (A — отличное, B — хорошее, C — среднее, D — плохое)

Краткое заключение (2-3 предложения)

Верни результат в JSON формате.

text
6. Отправить запрос в Gemini API (если ключ есть)
7. Если ключа нет — вернуть mock оценку (для тестирования)
8. Получить ответ от AI с новой оценкой
9. Обновить в заказе:
- condition_score (новый балл)
- grade (новый класс A/B/C/D)
- final_quality (новое значение)
- ai_summary (текст заключения)
10. Создать событие в timeline: "AI переоценил качество: B (78/100)"
11. Вернуть результат клиенту

**Важно:**
- Если Gemini API недоступен — использовать mock ответ
- Минимум 20 пунктов чеклиста должны быть заполнены
- Сохранять историю AI оценок (можно в отдельную таблицу)

---

## Задача 1.2: Frontend UI для чеклиста

### Компонент: InspectionChecklist

**Где:** frontend/src/components/crm/InspectionChecklist.tsx

**Как должен выглядеть:**

Это КЛЮЧЕВОЙ компонент всей CRM. Он должен быть интуитивным, быстрым и красивым.

**Общая структура:**
┌──────────────────────────────────────────────────────────┐
│ Чеклист проверки (28 пунктов) │
│ │
│ ████████████░░░░░░░░░░ 12 / 28 проверено (42.8%) │
│ │
├──────────────────────────────────────────────────────────┤
│ ✓ 1. Бренд подтвержден │
│ Комментарий: YT Industries проверен │
│ 📷 2 фото │
├──────────────────────────────────────────────────────────┤
│ ⏳ 2. Модель подтверждена │
│ В процессе проверки │
├──────────────────────────────────────────────────────────┤
│ ○ 3. Год подтвержден │
│ Не проверено │
├──────────────────────────────────────────────────────────┤
│ ... │
├──────────────────────────────────────────────────────────┤
│ [🤖 Запустить AI повторную оценку] │
└──────────────────────────────────────────────────────────┘

text

**Визуальные состояния пунктов:**
- ✓ Зеленая галочка = проверено (status: "checked")
- ⏳ Желтые часы = в процессе (status: "pending")
- ✗ Красный крестик = проблема найдена (status: "problem")
- ○ Серый круг = не проверено (status: null)

**Интерактивность:**
- Каждый пункт — кликабельный
- При клике открывается модальное окно редактирования
- Прогресс-бар обновляется в реальном времени

**Прогресс-бар:**
- Полоса с заливкой (зеленая)
- Текст: "12 / 28 проверено (42.8%)"
- Анимация при обновлении

**Кнопка AI оценки:**
- Видна всегда внизу чеклиста
- Disabled если проверено меньше 20 пунктов (с подсказкой почему)
- При клике показывает loader "AI анализирует..." (~10-15 секунд)
- После завершения — alert с результатом

**Поведение:**
- При загрузке страницы — запрос GET /inspection
- При клике на пункт — открытие модалки
- При сохранении в модалке — PATCH запрос и обновление UI
- При клике "AI оценка" — POST запрос и показ результата

**UX детали:**
- Пункты с комментариями показывают превью (первые 50 символов)
- Пункты с фото показывают иконку 📷 и количество
- Hover эффект на пунктах (светлый фон)
- Плавная анимация открытия модалки
- После сохранения — toast уведомление "Пункт обновлен"

---

### Компонент: ChecklistItemModal

**Где:** frontend/src/components/crm/ChecklistItemModal.tsx

**Как должен выглядеть:**

Модальное окно для редактирования одного пункта чеклиста.

**Структура:**
┌────────────────────────────────────────────────────┐
│ Состояние рамы [X] │
├────────────────────────────────────────────────────┤
│ │
│ Статус: │
│ ( ) ✓ Проверено ( ) ⏳ В процессе ( ) ✗ Проблема │
│ │
│ Комментарий: │
│ ┌──────────────────────────────────────────────┐ │
│ │ Рама в отличном состоянии, мелкие царапины │ │
│ │ от эксплуатации, виниловая защита. │ │
│ │ │ │
│ └──────────────────────────────────────────────┘ │
│ │
│ Фото (опционально): │
│ [📁 Выбрать файл] │
│ │
│ ┌─────┐ ┌─────┐ │
│ │ IMG │ │ IMG │ ← превью фото │
│ │ X │ │ X │ ← кнопка удалить │
│ └─────┘ └─────┘ │
│ │
│ [Отмена] [Сохранить] │
└────────────────────────────────────────────────────┘

text

**Элементы:**

1. **Заголовок:**
   - Название пункта (например "Состояние рамы")
   - Кнопка закрытия (X) справа

2. **Выбор статуса:**
   - Три больших radio button
   - Каждый с иконкой и текстом
   - Активный вариант подсвечен
   - Визуально крупные, легко кликать

3. **Textarea для комментария:**
   - Высота: 4-5 строк
   - Placeholder: "Опишите состояние детально..."
   - Автоматический resize если текст длинный

4. **Загрузка фото:**
   - Input type="file" с кнопкой "Выбрать файл"
   - При выборе — показывает "Загрузка..." 
   - Отправляет POST /inspection/upload
   - Получает URL и добавляет в список
   - Показывает превью загруженных фото (маленькие квадраты 80x80px)
   - У каждого превью — кнопка удалить (крестик)

5. **Кнопки действий:**
   - "Отмена" (вторичная, серая) — закрывает без сохранения
   - "Сохранить" (первичная, синяя) — отправляет PATCH запрос

**Поведение:**
- При открытии загружает текущие значения пункта
- При изменении статуса — обновляет визуальное состояние
- При загрузке фото — показывает прогресс
- При клике "Сохранить":
  - Валидирует что статус выбран
  - Отправляет PATCH запрос
  - Показывает loader на кнопке "Сохранение..."
  - При успехе — закрывает модалку и обновляет родительский компонент
  - При ошибке — показывает сообщение об ошибке

**UX детали:**
- Клик вне модалки НЕ закрывает её (только кнопка X или Отмена)
- Если есть несохраненные изменения и пользователь кликает X — спросить подтверждение
- Фокус автоматически на textarea при открытии
- Enter в textarea НЕ закрывает модалку (для многострочного текста)
- Анимация появления модалки (fade + slide)

---

### Вспомогательный файл: checklistLabels.ts

**Где:** frontend/src/utils/checklistLabels.ts

**Что содержит:**
Маппинг между техническими ключами из БД (например "6_frame_condition") 
и человекочитаемыми названиями на русском ("Состояние рамы").

Все 28 пунктов должны быть прописаны.

---

### Обновление API клиента

**Где:** frontend/src/api/crmManagerApi.ts

**Добавить методы:**
- getInspection(orderId) — GET запрос
- updateChecklistItem(orderId, item, status, comment, photos) — PATCH запрос
- uploadInspectionPhoto(orderId, formData) — POST запрос с файлом
- requestAIRegrade(orderId) — POST запрос

Все запросы должны включать JWT токен в заголовке Authorization.

---

## Задача 1.3: Интеграция чеклиста в страницу заказа

**Где:** frontend/src/pages/crm/orders/OrderDetailPage.tsx

**Что сделать:**
В детальной странице заказа уже есть место для чеклиста (левая колонка, после информации о байке).

**Требуется:**
1. Импортировать компонент InspectionChecklist
2. Разместить его в нужном месте layout
3. Передать orderId как prop
4. Убедиться что компонент растягивается на нужную ширину
5. Добавить возможность свернуть/развернуть чеклист (аккордеон)

**Дизайн:**
┌─────────────────────────────────────────────────────┐
│ Информация о велосипеде │
│ [Фото галерея] │
│ Название, характеристики, цена │
└─────────────────────────────────────────────────────┘

▼ Чеклист проверки (28 пунктов) [12/28 ✓] ← можно свернуть
┌─────────────────────────────────────────────────────┐
│ [Весь чеклист как описано выше] │
└─────────────────────────────────────────────────────┘

▼ Timeline событий
┌─────────────────────────────────────────────────────┐
│ [События] │
└─────────────────────────────────────────────────────┘

text

**Аккордеон:**
- По умолчанию развернут (если прогресс < 100%)
- Можно свернуть кликом на заголовок
- Показывать прогресс даже когда свернут
- Плавная анимация сворачивания

---

## 💡 ГЕНИАЛЬНЫЕ ИДЕИ (если считаешь нужным):

**Если ты видишь что чего-то не хватает для полноценного функционала чеклиста — внедри!**

Примеры возможных улучшений:
- Кнопка "Скопировать чеклист в буфер" (для отправки клиенту)
- Печать чеклиста в PDF
- История изменений пункта (кто и когда изменял)
- Возможность добавить свой кастомный пункт проверки
- Шаблоны комментариев (например "Рама в отличном состоянии" можно выбрать из списка)
- Голосовой ввод комментариев (через Web Speech API)
- Автосохранение черновика комментария (если менеджер начал писать но закрыл модалку)
- Подсветка пунктов с проблемами (красным)
- Уведомление если долго не заполняется (напоминание)
- Статистика: среднее время заполнения чеклиста
- Сравнение с другими заказами этого же бренда

**Важно:** Добавляй только то что реально улучшит работу менеджера, а не просто "фичи ради фич".

---

---

# 🟠 БЛОК 2: DRAG & DROP КАНБАН НА DASHBOARD

## Контекст проблемы

**Что сейчас:** На Dashboard есть визуальная "канбан доска" — 4 колонки с заказами 
(Новые брони, На проверке, Ожидают оплату, Готовы к отправке). Но это НЕ канбан. 
Это просто 4 статичных списка. Карточки заказов НЕ перетаскиваются.

**Оценка менеджера:** "Это критично. Канбан теряет смысл если это просто 4 списка. 
Мне нужно перетаскивать заказы между колонками. Сейчас я открываю каждый заказ отдельно 
и меняю статус вручную — это долго."

**Почему критично:** Канбан — это визуальный инструмент управления. Без drag & drop 
это бесполезный элемент интерфейса.

---

## Задача 2.1: Реализация Drag & Drop

### Выбор библиотеки

Используй библиотеку @dnd-kit (современная, легковесная, поддерживает React 18+).

**Установка:**
Добавь в frontend/package.json зависимости:
- @dnd-kit/core
- @dnd-kit/sortable
- @dnd-kit/utilities

---

### Как должен работать Drag & Drop

**Поведение:**
1. Менеджер наводит мышь на карточку заказа
2. Зажимает левую кнопку мыши
3. Карточка "поднимается" (визуально: небольшая тень, полупрозрачность)
4. Менеджер перетаскивает карточку в другую колонку
5. При наведении на целевую колонку — она подсвечивается (например зеленой рамкой)
6. Менеджер отпускает кнопку мыши
7. Карточка "падает" в новую колонку с анимацией
8. Отправляется PATCH запрос на сервер для изменения статуса заказа
9. Если запрос успешный — карточка остается в новой колонке
10. Если запрос failed — карточка возвращается обратно с анимацией + показывается ошибка

**Визуальная обратная связь:**
- Курсор меняется на "grab" при наведении
- Курсор меняется на "grabbing" при перетаскивании
- Карточка становится полупрозрачной (opacity: 0.6)
- Под карточкой появляется тень
- Целевая колонка подсвечивается
- Остальные карточки в целевой колонке "расступаются" (анимация)

**Ограничения:**
- Нельзя перетащить заказ в колонку "Delivered" или "Cancelled" (это финальные статусы)
- Нельзя перетащить заказ "назад" по воронке (например из "Готовы к отправке" в "Новые брони")
- При попытке — показывать tooltip с объяснением

---

### Backend: Обновление статуса через Drag & Drop

**Endpoint уже существует:** PATCH /api/v1/crm/orders/:orderId/status

**Что нужно доработать:**
- Принимать новый статус из body запроса
- Валидировать что статус разрешен (нельзя назад по воронке)
- Обновлять статус в БД
- Создавать событие в timeline: "Статус изменен: под проверкой → ожидает оплату (через канбан)"
- Логировать в audit_log
- Возвращать success

**Логика валидации:**
Определить порядок статусов в воронке:
1. pending_manager (Новые брони)
2. under_inspection (На проверке)
3. awaiting_deposit (Ожидают оплату)
4. deposit_paid (Оплачен аванс)
5. ready_for_shipment (Готовы к отправке)
6. in_transit (В пути)
7. delivered (Доставлено) — финальный
8. cancelled (Отменен) — финальный

Разрешить перемещение только "вперед" или в пределах соседних статусов.

---

### Frontend: Компоненты для Drag & Drop

#### DashboardPage.tsx

**Что изменить:**
- Обернуть канбан в DndContext (из @dnd-kit/core)
- Реализовать обработчик onDragEnd
- При успешном перетаскивании:
  - Оптимистично обновить UI (переместить карточку сразу)
  - Отправить PATCH запрос
  - Если запрос failed — откатить UI и показать ошибку
- При неудачном перетаскивании (например невалидная колонка) — показать tooltip

#### KanbanColumn.tsx

**Создать компонент для одной колонки канбана:**

**Что должен содержать:**
- Заголовок колонки (название статуса)
- Счетчик заказов в колонке
- Список карточек заказов
- Droppable зону (куда можно "бросить" карточку)

**Визуальное оформление:**
┌─────────────────────────────┐
│ Новые брони (5) │ ← заголовок + счетчик
├─────────────────────────────┤
│ ┌─────────────────────────┐ │
│ │ [фото байка] │ │
│ │ ORD-001 │ │
│ │ Canyon Spectral 2021 │ │
│ │ Дмитрий │ │
│ │ 2500 EUR │ │
│ └─────────────────────────┘ │
│ │
│ ┌─────────────────────────┐ │
│ │ [следующая карточка] │ │
│ └─────────────────────────┘ │
│ │
│ ... еще карточки │
└─────────────────────────────┘

text

**Состояния колонки:**
- Обычное (серый бордер)
- Hover при перетаскивании (зеленый бордер)
- Активное (карточка над колонкой) (зеленый фон полупрозрачный)

#### KanbanCard.tsx

**Создать компонент для карточки заказа:**

**Что должен содержать:**
- Фото велосипеда (маленькое, 80x60px)
- Order Code (ORD-001)
- Название велосипеда (одна строка, если длинное — обрезать с ...)
- Имя клиента
- Сумма заказа (крупным шрифтом)
- Бейдж срочности (если заказ просрочен или требует внимания)

**Визуальное оформление:**
- Белая карточка с тенью
- Скругленные углы (8px)
- Padding 12px
- Hover эффект: тень увеличивается
- При перетаскивании: полупрозрачность + большая тень

**Интерактивность:**
- Draggable (можно перетаскивать)
- Кликабельная (клик открывает детальную страницу заказа)
- Если перетаскивается — клик не срабатывает

**Бейджи срочности:**
- 🔴 Красный: заказ просрочен (создан > 7 дней назад, всё ещё в pending_manager)
- 🟡 Желтый: требует внимания (в under_inspection > 3 дней)
- 🔵 Синий: VIP клиент (если клиент делал >3 заказов)

---

## Задача 2.2: Дополнительные улучшения канбана

### Фильтрация канбана

Добавить над канбаном фильтры:
- "Только мои заказы" (toggle switch)
- "Скрыть старые" (скрыть заказы старше 14 дней)
- Поиск по Order Code или названию байка

### Лимит карточек в колонке

В каждой колонке показывать максимум 10 карточек. Если больше — кнопка "Показать еще (+5)".

Это ускоряет загрузку и не перегружает экран.

### Звуковая обратная связь (опционально)

При успешном перемещении заказа — легкий звук "plop" (можно использовать Web Audio API).

Это создает тактильное ощущение работы с физическими карточками.

---

## 💡 ГЕНИАЛЬНЫЕ ИДЕИ (если считаешь нужным):

**Если видишь что можно улучшить канбан — делай!**

Примеры:
- Bulk drag & drop (выбрать несколько карточек и перетащить разом)
- Автоматическое перемещение заказа если прошло событие (например оплата пришла → автоматом в "Оплачен аванс")
- Анимация "пролетающей" карточки между колонками
- Свернуть/развернуть колонку (если она не нужна сейчас)
- Кастомизация колонок (какие статусы показывать)
- Цветовая кодировка колонок (каждая своим цветом)
- Счетчик суммы всех заказов в колонке (показывает потенциальную выручку)

---

---

# 🟡 БЛОК 3: КНОПКИ СВЯЗИ С КЛИЕНТОМ

## Контекст проблемы

**Что сейчас:** В карточке заказа отображается имя клиента, телефон и email. 
Но это просто текст. Нельзя кликнуть и позвонить. Нельзя кликнуть и открыть WhatsApp. 
Менеджеру приходится копировать номер, открывать телефон/мессенджер, вставлять номер вручную.

**Оценка менеджера:** "Я трачу время на copy-paste номера. Это раздражает. 
Мне нужны кнопки 'Позвонить', 'WhatsApp', 'Email' в один клик."

**Почему критично:** Менеджер звонит/пишет клиентам 10-20 раз в день. 
Каждая лишняя секунда умножается на сотни действий.

---

## Задача 3.1: Интерактивные контакты клиента

### Где реализовать

В детальной странице заказа (OrderDetailPage.tsx) в блоке "Клиент" (правая колонка).

**Текущий вид:**
┌────────────────────────┐
│ Клиент │
├────────────────────────┤
│ Дмитрий Иванов │
│ +7 999 123-45-67 │
│ dmitry@example.com │
│ Москва │
└────────────────────────┘

text

**Новый вид:**
┌────────────────────────────────────────┐
│ Клиент │
├────────────────────────────────────────┤
│ Дмитрий Иванов │
│ │
│ Телефон: │
│ +7 999 123-45-67 │
│ [📞 Позвонить] [💬 WhatsApp] [📋 Копировать] │
│ │
│ Email: │
│ dmitry@example.com │
│ [✉️ Написать] [📋 Копировать] │
│ │
│ Telegram: │
│ @dmitry_ivanov │
│ [✈️ Открыть чат] │
│ │
│ Город: Москва │
│ │
│ [✉️ Написать клиенту] ← большая кнопка │
└────────────────────────────────────────┘

text

---

### Кнопка "Позвонить"

**Что делает:**
Открывает приложение для звонков на устройстве.

**Как реализовать:**
Использовать `<a href="tel:+79991234567">` или `window.open('tel:...')`

**UX:**
- Иконка 📞
- Текст "Позвонить"
- На мобильном — открывает приложение телефона
- На десктопе — открывает Skype/Zoom/другие приложения если установлены

---

### Кнопка "WhatsApp"

**Что делает:**
Открывает чат с клиентом в WhatsApp (веб или приложение).

**Как реализовать:**
`https://wa.me/79991234567`

**Важно:**
- Номер должен быть в международном формате без + (79991234567)
- Если номер в формате "+7 (999) 123-45-67" — очистить от всех символов кроме цифр

**UX:**
- Иконка 💬
- Текст "WhatsApp"
- Открывается в новой вкладке
- Если WhatsApp не установлен — откроется веб-версия

---

### Кнопка "Копировать" (для телефона)

**Что делает:**
Копирует номер телефона в буфер обмена.

**Как реализовать:**
`navigator.clipboard.writeText(phone)`

**UX:**
- Иконка 📋
- Текст "Копировать"
- После клика — показать toast уведомление "Номер скопирован"
- Иконка меняется на ✓ на 2 секунды

---

### Кнопка "Написать Email"

**Что делает:**
Открывает почтовый клиент с предзаполненным письмом.

**Как реализовать:**
`mailto:dmitry@example.com?subject=Заказ ORD-001&body=Здравствуйте, Дмитрий!`

**UX:**
- Иконка ✉️
- Текст "Написать"
- Предзаполнить тему письма: "Заказ [Order Code]"
- Предзаполнить приветствие: "Здравствуйте, [Имя клиента]!"

---

### Кнопка "Копировать" (для email)

**Аналогично кнопке для телефона.**

---

### Кнопка "Открыть Telegram"

**Что делает:**
Открывает чат с клиентом в Telegram.

**Как реализовать:**
`https://t.me/dmitry_ivanov` (без @)

**UX:**
- Иконка ✈️
- Текст "Открыть чат"
- Открывается в новой вкладке

**Важно:**
- Если у клиента нет Telegram username — не показывать эту кнопку
- Username хранится в БД в таблице customers

---

### Большая кнопка "Написать клиенту"

**Что делает:**
Открывает модальное окно для выбора способа связи (Email / WhatsApp / Telegram).

**Зачем:**
Иногда менеджер не знает каким способом лучше связаться. Эта кнопка дает выбор.

**Модалка:**
┌────────────────────────────────────────┐
│ Выберите способ связи │
├────────────────────────────────────────┤
│ [✉️ Email] Быстрый ответ │
│ [💬 WhatsApp] Неформальное общение │
│ [✈️ Telegram] Мгновенная связь │
├────────────────────────────────────────┤
│ Быстрые шаблоны: │
│ - "Байк поступил на склад" │
│ - "Ожидаем оплату" │
│ - "Готов к отправке" │
│ - "Отправлено, трек-номер: ..." │
└────────────────────────────────────────┘

text

При выборе способа — открывается соответствующее приложение с предзаполненным текстом из шаблона.

---

## 💡 ГЕНИАЛЬНЫЕ ИДЕИ (если считаешь нужным):

Примеры:
- История коммуникаций (показывать когда последний раз писали клиенту)
- Кнопка "Напомнить позвонить" (создает задачу с дедлайном)
- Интеграция с VoIP (если в компании используется)
- Автоматические шаблоны в зависимости от статуса заказа
- Tracking открытий email (если клиент прочитал письмо)

---

---

# 🟢 БЛОК 4: РЕДАКТИРУЕМАЯ ЛОГИСТИКА

## Контекст проблемы

**Что сейчас:** В детальной странице заказа есть блок "Логистика" с информацией о доставке.
Но он "read-only". Менеджер не может вписать tracking номер. Не может обновить статус доставки.
Не может отметить "Получено на складе".

**Оценка менеджера:** "Я не могу сообщить клиенту трек-номер через систему. 
Приходится писать в WhatsApp отдельно. А в CRM данные устаревшие."

**Почему критично:** Логистика — важная часть заказа. Клиент постоянно спрашивает "Где мой байк?".
Менеджер должен быстро обновлять информацию.

---

## Задача 4.1: Backend для логистики

### Endpoint: Обновление данных доставки

**Метод:** PATCH  
**Путь:** /api/v1/crm/shipments/:shipmentId

**Что принимает:**
- tracking_number (строка)
- carrier (строка, например "DHL", "CDEK")
- status (строка: "pending" | "in_transit" | "delivered")
- warehouse_received (boolean)
- delivery_notes (текст)

**Что делает:**
1. Найти shipment в БД по ID
2. Обновить поля
3. Создать событие в timeline: "Добавлен трек-номер: ABC123"
4. Если статус изменился — обновить статус заказа
5. Логировать в audit_log
6. Вернуть обновленные данные

**Логика связи с заказом:**
- Если shipment.status = "delivered" → order.status = "delivered"
- Если shipment.warehouse_received = true → создать задачу "Провести финальную проверку"

---

### Endpoint: Отправка трек-номера клиенту

**Метод:** POST  
**Путь:** /api/v1/crm/orders/:orderId/notify-tracking

**Что делает:**
Автоматически отправляет email или SMS клиенту с трек-номером.

**Тело запроса:**
- method ("email" | "sms" | "whatsapp")

**Логика:**
1. Получить заказ и shipment
2. Проверить что tracking_number заполнен
3. Получить контакты клиента
4. Сформировать сообщение:
Здравствуйте, [Имя]!

Ваш велосипед [Название] отправлен.
Трек-номер: [Tracking]
Перевозчик: [Carrier]

Отслеживание: [ссылка]

С уважением,
BikeWerk

text
5. Отправить через выбранный канал
6. Создать событие в timeline: "Трек-номер отправлен клиенту (email)"
7. Вернуть success

---

## Задача 4.2: Frontend для логистики

### Обновить блок "Логистика" в OrderDetailPage

**Текущий вид:**
┌────────────────────────┐
│ Логистика │
├────────────────────────┤
│ Способ: Доставка │
│ Трек-номер: - │
│ Статус: Ожидает │
└────────────────────────┘

text

**Новый вид:**
┌──────────────────────────────────────────┐
│ Логистика │
├──────────────────────────────────────────┤
│ Способ доставки: │
│ [Dropdown: Доставка / Самовывоз] │
│ │
│ Перевозчик: │
│ [Input: DHL] │
│ │
│ Трек-номер: │
│ [Input: ABC123456789] │
│ [🔗 Отследить] [✉️ Отправить клиенту] │
│ │
│ Статус: │
│ [Dropdown: Ожидает / В пути / Доставлено]│
│ │
│ Чекбоксы: │
│ ☑ Получено на складе │
│ ☐ Фото сделаны │
│ ☐ Клиент получил │
│ │
│ Заметки: │
│ [Textarea] │
│ │
│ [Сохранить изменения] │
└──────────────────────────────────────────┘

text

---

### Элементы блока

**1. Dropdown "Способ доставки":**
- Варианты: Доставка / Самовывоз / Неизвестно
- При изменении — автосохранение

**2. Input "Перевозчик":**
- Автозаполнение из списка популярных (DHL, CDEK, Почта России, СДЭК)
- Можно ввести вручную

**3. Input "Трек-номер":**
- При вводе — кнопка "Сохранить" подсвечивается
- После сохранения — активируются кнопки "Отследить" и "Отправить клиенту"

**4. Кнопка "Отследить":**
- Открывает сайт перевозчика с трек-номером
- Умные ссылки:
  - DHL: `https://www.dhl.com/tracking?AWB={tracking}`
  - CDEK: `https://www.cdek.ru/track.html?order_id={tracking}`
  - И т.д. для популярных перевозчиков

**5. Кнопка "Отправить клиенту":**
- Открывает модалку выбора способа (Email / SMS / WhatsApp)
- Отправляет POST /notify-tracking
- Показывает toast "Трек-номер отправлен"

**6. Dropdown "Статус":**
- Варианты: Ожидает отправки / В пути / Доставлено
- При изменении — обновляется статус заказа

**7. Чекбоксы:**
- Состояния доставки
- При изменении — автосохранение
- Если все чекбоксы отмечены — предложить завершить заказ

**8. Textarea "Заметки":**
- Для внутренних комментариев менеджера
- Не видны клиенту

**9. Кнопка "Сохранить изменения":**
- Активна если есть несохраненные изменения
- При клике — PATCH запрос
- Toast уведомление "Логистика обновлена"

---

### Автосохранение vs Ручное сохранение

**Решение:**
- Простые поля (чекбоксы, dropdown) — автосохранение
- Текстовые поля (трек-номер, заметки) — ручное сохранение (кнопка)

Это баланс между удобством и безопасностью (чтобы не потерять данные при случайном закрытии).

---

## 💡 ГЕНИАЛЬНЫЕ ИДЕИ (если считаешь нужным):

Примеры:
- Автоматическое определение перевозчика по формату трек-номера
- Webhook от перевозчика (обновление статуса в реальном времени)
- История перемещения посылки (если API перевозчика поддерживает)
- Оценка времени доставки (по статистике)
- Уведомление менеджера когда посылка задерживается
- Интеграция с картой (показать где сейчас посылка)

---

---

# 🟢 БЛОК 5: РЕДАКТИРУЕМЫЕ ФИНАНСЫ

## Контекст проблемы

**Что сейчас:** Блок "Финансы" показывает разбивку цены (байк, доставка, комиссия, итого).
Но это статичные данные из bike_snapshot. Менеджер не может добавить транзакцию вручную
(например "Клиент доплатил 50 EUR наличными" или "Возврат 100 EUR за дефект").

**Оценка менеджера:** "Иногда оплаты приходят частями или нестандартно. 
Я не могу это отразить в системе."

**Почему критично:** Финансовая прозрачность важна для учета и отчетности.

---

## Задача 5.1: Backend для финансов

### Endpoint: Добавление транзакции

**Метод:** POST  
**Путь:** /api/v1/crm/orders/:orderId/transactions

**Что принимает:**
- amount (число, EUR)
- type ("payment" | "refund" | "fee" | "adjustment")
- method ("card" | "bank_transfer" | "cash" | "paypal")
- description (текст)
- transaction_date (дата, опционально — по умолчанию сейчас)

**Что делает:**
1. Создать запись в таблице transactions (или order_transactions)
2. Обновить общую сумму оплаченного в заказе
3. Создать событие в timeline: "Добавлена транзакция: +50 EUR (наличные)"
4. Логировать в audit_log
5. Если сумма оплаченного >= total_amount → обновить статус заказа на "deposit_paid" или далее
6. Вернуть обновленный список транзакций

---

### Endpoint: Получение транзакций заказа

**Метод:** GET  
**Путь:** /api/v1/crm/orders/:orderId/transactions

**Что возвращает:**
Список всех транзакций по заказу с полями:
- id
- amount
- type
- method
- description
- created_at
- created_by (менеджер)

---

### Endpoint: Удаление транзакции

**Метод:** DELETE  
**Путь:** /api/v1/crm/transactions/:transactionId

**Что делает:**
1. Найти транзакцию
2. Проверить что она не финальная (нельзя удалить транзакцию если заказ уже закрыт)
3. Удалить или пометить как deleted
4. Обновить общую сумму в заказе
5. Создать событие в timeline: "Транзакция удалена: -50 EUR"
6. Логировать
7. Вернуть success

---

## Задача 5.2: Frontend для финансов

### Обновить блок "Финансы" в OrderDetailPage

**Текущий вид:**
┌────────────────────────┐
│ Финансы │
├────────────────────────┤
│ Байк: 2500 EUR │
│ Доставка: 150 EUR │
│ Комиссия: 250 EUR │
│ Итого: 2900 EUR │
└────────────────────────┘

text

**Новый вид:**
┌──────────────────────────────────────────┐
│ Финансы │
├──────────────────────────────────────────┤
│ Разбивка цены: │
│ - Байк: 2500 EUR │
│ - Доставка: 150 EUR │
│ - Комиссия (10%): 250 EUR │
│ - Доп услуги: 0 EUR │
│ ────────────────────────── │
│ ИТОГО: 2900 EUR │
│ │
│ Оплачено: 2900 EUR (100%) ✓ │
│ Осталось: 0 EUR │
│ │
│ История платежей: │
│ ┌──────────────────────────────────────┐ │
│ │ 2026-02-01 Резерв (2%) 58 EUR │ │
│ │ 2026-02-05 Основной платеж 2842 EUR│ │
│ │ 2026-02-06 Наличные 50 EUR [✕] │ │ ← можно удалить
│ └──────────────────────────────────────┘ │
│ │
│ [+ Добавить транзакцию] │
└──────────────────────────────────────────┘

text

---

### Кнопка "Добавить транзакцию"

При клике открывает модалку:

┌────────────────────────────────────────┐
│ Добавить транзакцию │
├────────────────────────────────────────┤
│ Сумма: │
│ [Input: ] EUR │
│ │
│ Тип: │
│ ( ) Оплата ( ) Возврат ( ) Доплата │
│ │
│ Метод: │
│ [Dropdown: Карта / Перевод / Наличные] │
│ │
│ Описание: │
│ [Textarea] │
│ │
│ Дата: │
│ [Date picker, по умолчанию сегодня] │
│ │
│ [Отмена] [Добавить] │
└────────────────────────────────────────┘

text

После сохранения:
- POST запрос
- Обновление списка транзакций
- Пересчет "Оплачено" и "Осталось"
- Toast уведомление

---

### Индикатор оплаты

**Визуализация:**
Оплачено: ████████████░░ 2900 / 3000 EUR (96%)

text

Цвет индикатора:
- Красный: оплачено < 50%
- Желтый: оплачено 50-99%
- Зеленый: оплачено 100%

---

### История платежей

**Формат:**
Каждая транзакция отображается строкой:
- Дата
- Описание
- Сумма (+ или - в зависимости от типа)
- Кнопка удалить (маленький крестик)

**Сортировка:** от новых к старым

**Удаление:**
- При клике на крестик — confirm диалог "Удалить транзакцию?"
- После подтверждения — DELETE запрос
- Обновление списка

---

## 💡 ГЕНИАЛЬНЫЕ ИДЕИ (если считаешь нужным):

Примеры:
- Автоматический импорт транзакций из PayPal/Stripe (если есть API)
- Шаблоны транзакций ("Резерв 2%", "Основной платеж")
- Напоминание клиенту об оплате (если не оплачено через 3 дня)
- Экспорт финансового отчета по заказу (PDF)
- Статистика: сколько клиент потратил всего (если это повторный заказ)
- Прогноз прибыли (с учетом комиссий и расходов)

---

---

# 🔵 БЛОК 6: ЭКСПОРТ ЗАКАЗОВ В EXCEL/CSV

## Контекст проблемы

**Что сейчас:** Нет возможности выгрузить список заказов в Excel. 
Менеджер не может сделать отчет для руководства.

**Оценка менеджера:** "Руководство просит отчеты каждую неделю. 
Я не могу выгрузить данные. Приходится вручную копировать в Excel."

**Почему критично:** Отчетность — обязательная часть работы.

---

## Задача 6.1: Backend для экспорта

### Endpoint: Экспорт заказов

**Метод:** GET  
**Путь:** /api/v1/crm/orders/export

**Query параметры:**
- format ("csv" | "excel")
- filters (те же что в GET /orders: status, manager, date_from, date_to)

**Что делает:**
1. Получить заказы с примененными фильтрами
2. Сформировать файл (CSV или Excel)
3. Включить колонки:
   - Order Code
   - Order Date
   - Customer Name
   - Customer City
   - Bike Name
   - Status
   - Manager
   - Total Amount (EUR)
   - Paid Amount (EUR)
   - Tracking Number
4. Вернуть файл для скачивания
5. Content-Disposition: attachment; filename="orders_export_2026-02-06.csv"

**Для CSV:**
Простой текстовый формат, разделитель — запятая.

**Для Excel (опционально):**
Использовать библиотеку типа `exceljs` (Node.js).

---

## Задача 6.2: Frontend для экспорта

### Кнопка экспорта в списке заказов

**Где:** OrdersListPage.tsx, над таблицей (справа от фильтров)

**Визуально:**
[Фильтры...] [📥 Экспорт в Excel ▼]

text

**При клике:**
Dropdown меню:
- Экспорт в CSV
- Экспорт в Excel (если реализовано)

**Логика:**
1. При выборе формата — показать loader "Формирование файла..."
2. GET запрос /orders/export?format=csv&[текущие фильтры]
3. Браузер автоматически скачивает файл
4. Toast уведомление "Файл скачан"

**Важно:**
Экспортируются только те заказы которые видны с текущими фильтрами. 
Это позволяет делать выборочные отчеты.

---

### Экспорт детального заказа (опционально)

**Где:** OrderDetailPage.tsx, в меню действий (три точки)

**Что экспортирует:**
Полную информацию о заказе в PDF или Excel:
- Информация о байке
- Чеклист
- Финансы
- Логистика
- Timeline

Это нужно для архивирования или отправки клиенту.

---

## 💡 ГЕНИАЛЬНЫЕ ИДЕИ (если считаешь нужным):

Примеры:
- Кастомизация колонок экспорта (выбрать какие поля включить)
- Экспорт в Google Sheets (прямая интеграция)
- Автоматический еженедельный отчет на email руководству
- Экспорт с графиками (Excel с встроенными charts)
- Темплейты экспорта (сохранить набор фильтров + колонок)

---

---

# 🔵 БЛОК 7: МАССОВЫЕ ДЕЙСТВИЯ В ТАБЛИЦЕ ЗАКАЗОВ

## Контекст проблемы

**Что сейчас:** В таблице заказов нет чекбоксов. Нельзя выбрать несколько заказов 
и применить действие к ним сразу (например назначить менеджера или изменить статус).

**Оценка менеджера:** "Менять статус по одному заказу долго. 
У меня может быть 10 новых заказов утром — я хочу всем назначить себя одним кликом."

**Почему критично:** Экономия времени при рутинных действиях.

---

## Задача 7.1: Backend для массовых действий

### Endpoint: Массовое изменение статуса

**Метод:** PATCH  
**Путь:** /api/v1/crm/orders/bulk/status

**Тело запроса:**
- order_ids (массив ID заказов)
- new_status (строка)

**Что делает:**
1. Валидировать что все заказы существуют
2. Обновить статус для всех заказов
3. Создать события в timeline для каждого
4. Логировать в audit_log
5. Вернуть количество обновленных заказов

---

### Endpoint: Массовое назначение менеджера

**Метод:** PATCH  
**Путь:** /api/v1/crm/orders/bulk/assign

**Тело запроса:**
- order_ids (массив)
- manager_id (ID менеджера)

**Аналогично.**

---

## Задача 7.2: Frontend для массовых действий

### Чекбоксы в таблице

**Что добавить:**
- Колонка с чекбоксами (первая слева)
- Чекбокс в заголовке таблицы ("Выбрать все")
- Чекбокс у каждой строки

**Логика:**
- При клике на чекбокс строки — добавить/убрать ID из списка выбранных
- При клике "Выбрать все" — выбрать все заказы на текущей странице
- Показывать счетчик выбранных (например "Выбрано: 5")

---

### Плавающая панель массовых действий

**Где:** Внизу экрана (fixed position)

**Когда появляется:** Когда выбран хотя бы 1 заказ

**Визуально:**
┌────────────────────────────────────────────────────────────────┐
│ Выбрано: 5 заказов │
│ │
│ [👤 Назначить менеджера ▼] [📊 Изменить статус ▼] [✕ Отменить]│
└────────────────────────────────────────────────────────────────┘

text

**Кнопки:**

**1. "Назначить менеджера"**
- Dropdown со списком всех менеджеров
- При выборе — confirm диалог "Назначить [Имя] на 5 заказов?"
- После подтверждения — PATCH /bulk/assign
- Toast "Менеджер назначен на 5 заказов"
- Снять выделение

**2. "Изменить статус"**
- Dropdown со списком статусов
- Аналогично

**3. "Отменить"**
- Снять все выделения
- Скрыть панель

---

### UX детали

**Сохранение выделения:**
- При переходе между страницами таблицы — выделение НЕ сбрасывается
- Можно выбрать 5 заказов на странице 1, перейти на страницу 2, выбрать еще 3 — итого 8 выбранных

**Лимит выделения:**
Максимум 50 заказов за раз (чтобы не перегружать сервер).

**Визуальная обратная связь:**
- Выбранные строки подсвечены (светло-синий фон)
- Анимация появления панели снизу (slide up)

---

## 💡 ГЕНИАЛЬНЫЕ ИДЕИ (если считаешь нужным):

Примеры:
- Быстрые действия: "Назначить мне", "Взять в работу" (статус + менеджер одним кликом)
- Массовый экспорт выбранных заказов
- Массовая отправка уведомлений клиентам
- "Умный выбор": выбрать все заказы старше N дней
- История массовых действий (для отмены)

---

---

# 🔵 БЛОК 8: СОХРАНЕНИЕ ФИЛЬТРОВ В URL

## Контекст проблемы

**Что сейчас:** При применении фильтров на странице заказов (например статус="under_inspection", 
manager="Иван") эти фильтры НЕ сохраняются в URL. При перезагрузке страницы фильтры сбрасываются.

**Оценка менеджера:** "Бесит каждый раз настраивать 'Мои заказы' + 'В работе'. 
Я теряю контекст."

**Почему критично:** Менеджер работает с одними и теми же фильтрами весь день. 
Каждая перезагрузка = потеря времени.

---

## Задача 8.1: URL Query Parameters

### Как должно работать

**Без фильтров:**
/crm/orders

text

**С фильтрами:**
/crm/orders?status=under_inspection&manager=123&date_from=2026-02-01&date_to=2026-02-06

text

**При перезагрузке:**
Страница читает query params из URL и автоматически применяет фильтры.

**При изменении фильтра:**
URL обновляется через `setSearchParams()` (React Router).

---

### Frontend реализация

**Где:** OrdersListPage.tsx

**Что изменить:**

1. **При загрузке страницы:**
   - Прочитать query params из URL
   - Применить их как начальные значения фильтров
   - Загрузить заказы с этими фильтрами

2. **При изменении фильтра:**
   - Обновить state фильтров
   - Обновить URL через `searchParams.set(key, value)`
   - Загрузить заказы

3. **При сбросе фильтров:**
   - Очистить state
   - Очистить URL (вернуться к `/crm/orders`)
   - Загрузить заказы без фильтров

**Код структура:**
```typescript
const [searchParams, setSearchParams] = useSearchParams();

// При загрузке
useEffect(() => {
  const status = searchParams.get('status');
  const manager = searchParams.get('manager');
  // ... применить фильтры
  loadOrders({ status, manager });
}, [searchParams]);

// При изменении фильтра
const handleFilterChange = (key, value) => {
  searchParams.set(key, value);
  setSearchParams(searchParams);
};

// При сбросе
const handleResetFilters = () => {
  setSearchParams({});
};
Дополнительно: Сохранение в localStorage
Зачем:
Если менеджер хочет чтобы фильтры "запоминались" даже при переходе на другие страницы
и возврате обратно.

Реализация:

typescript
// При изменении фильтров
localStorage.setItem('crm_orders_filters', JSON.stringify(filters));

// При загрузке страницы (если нет query params)
const savedFilters = localStorage.getItem('crm_orders_filters');
if (savedFilters && !searchParams.toString()) {
  const filters = JSON.parse(savedFilters);
  setSearchParams(filters);
}
💡 ГЕНИАЛЬНЫЕ ИДЕИ (если считаешь нужным):
Примеры:

Закладки фильтров ("Мои срочные", "Новые из Москвы")

Шаринг фильтров (скопировать URL и отправить коллеге)

Preset фильтры ("Сегодняшние", "На этой неделе", "Проблемные")

История фильтров (последние 5 использованных комбинаций)

🔵 БЛОК 9: БЫСТРОЕ СОЗДАНИЕ ЗАДАЧИ ИЗ ЗАКАЗА
Контекст проблемы
Что сейчас: Если менеджер видит проблему в заказе и хочет создать задачу
(например "Перезвонить клиенту завтра"), ему нужно:

Открыть раздел "Задачи"

Кликнуть "Создать задачу"

Заполнить форму

Вручную выбрать заказ из dropdown

Сохранить

Оценка менеджера: "Я вижу проблему в заказе, хочу поставить задачу 'Перезвонить',
но надо идти в раздел 'Задачи'. Это неудобно."

Почему критично: Контекстные действия должны быть быстрыми.

Задача 9.1: Виджет быстрой задачи
Где добавить
Место 1: В детальной странице заказа (OrderDetailPage.tsx) в блоке "Задачи" (правая колонка)

Место 2: В списке заказов (OrdersListPage.tsx) в контекстном меню строки (три точки)

Виджет в OrderDetailPage
Визуально:

text
┌────────────────────────────────────┐
│ Задачи по заказу              (3)  │
├────────────────────────────────────┤
│ ✓ Уточнить цвет рамы               │
│ ⏳ Запросить фото дефектов         │
│ ○ Отправить инвойс                 │
├────────────────────────────────────┤
│ Быстрая задача:                    │
│ [Input: Что нужно сделать?]   [+]  │
└────────────────────────────────────┘
Как работает:

Менеджер вводит текст задачи (например "Перезвонить клиенту")

Нажимает Enter или кнопку "+"

Задача создается:

title = введенный текст

order_id = текущий заказ

assigned_to = текущий менеджер (из JWT)

priority = "normal"

deadline = null (можно добавить позже)

POST /crm/tasks

Задача добавляется в список

Input очищается

Toast "Задача создана"

Контекстное меню в таблице заказов
При клике на три точки в строке:

text
┌──────────────────────────┐
│ Открыть детали           │
│ Назначить менеджера      │
│ Изменить статус          │
├──────────────────────────┤
│ ✚ Создать задачу         │
└──────────────────────────┘
При выборе "Создать задачу":
Открывается маленькая модалка:

text
┌────────────────────────────────┐
│ Быстрая задача                 │
├────────────────────────────────┤
│ Заказ: ORD-001 (Canyon Spectral)│
│                                │
│ Задача:                        │
│ [Input: Что нужно сделать?]    │
│                                │
│ Приоритет:                     │
│ ( ) Обычный  ( ) Срочный       │
│                                │
│ Срок:                          │
│ [Date picker, опционально]     │
│                                │
│     [Отмена]  [Создать]       │
└────────────────────────────────┘
Шаблоны быстрых задач
Идея:
Под input добавить кнопки с шаблонами:

text
Быстрые задачи:
[📞 Перезвонить] [✉️ Отправить инвойс] [📦 Проверить склад]
При клике на шаблон — задача создается моментально (без ввода текста).

💡 ГЕНИАЛЬНЫЕ ИДЕИ (если считаешь нужным):
Примеры:

Голосовой ввод задачи (Web Speech API)

Автоматические задачи при изменении статуса (если статус = "awaiting_deposit" → создать задачу "Напомнить об оплате через 2 дня")

Повторяющиеся задачи (например "Обновить клиента" каждые 3 дня)

Задачи с подзадачами

Интеграция с календарем (задача = событие в календаре)

🔵 БЛОК 10: UX ПОЛИРОВКА
Контекст проблемы
Что сейчас: Мелкие UX проблемы которые в сумме создают дискомфорт:

Мелкие кликабельные зоны (трудно попасть мышкой)

Неочевидные интерактивные элементы

Отсутствие feedback при действиях

Медленная загрузка без индикаторов

Оценка менеджера: "Мелкие кликабельные зоны. Названия байков и статусы мелкие,
трудно попасть мышкой."

Почему критично: UX — это сумма мелочей. Даже если функционал работает,
плохой UX убивает продуктивность.

Задача 10.1: Увеличить кликабельные зоны
В таблице заказов
Что изменить:

Высота строки: минимум 60px (сейчас ~40px)

Padding строки: 16px (сейчас ~8px)

Размер шрифта Order Code: 16px (сейчас ~14px)

Размер шрифта названия байка: 15px

Вся строка кликабельна (не только Order Code)

Hover эффект:

При наведении на строку — легкий фон (светло-серый)

Курсор меняется на pointer

Легкая тень появляется

В канбан карточках
Что изменить:

Минимальная высота карточки: 140px

Padding: 16px

Размер шрифта Order Code: 14px bold

Размер шрифта названия байка: 13px

Вся карточка кликабельна

В модалках и формах
Что изменить:

Размер кнопок: минимум 44x44px (стандарт для touch)

Поля ввода: минимальная высота 44px

Radio buttons и checkboxes: минимум 24x24px (с большим padding)

Задача 10.2: Индикаторы загрузки
Где добавить
1. При загрузке страницы:

text
┌────────────────────────────────┐
│                                │
│     ⏳ Загрузка заказов...     │
│                                │
└────────────────────────────────┘
2. При отправке формы:
Кнопка "Сохранить" → "Сохранение..." + spinner

3. При загрузке чеклиста:
Skeleton loader (серые прямоугольники на месте пунктов)

4. При Drag & Drop:
Полупрозрачный overlay "Обновление..."

Задача 10.3: Toast уведомления
Система уведомлений
Создать компонент: Toast.tsx

Типы:

Success (зеленый) ✓

Error (красный) ✗

Warning (желтый) ⚠

Info (синий) ℹ

Позиция: Верх справа

Автоматическое скрытие: Через 4 секунды

Примеры использования:

"Заказ обновлен" (success)

"Ошибка сохранения" (error)

"Внимание: заказ просрочен" (warning)

"Новый заказ добавлен" (info)

Задача 10.4: Пустые состояния
Когда список заказов пуст
Вместо пустой таблицы показать:

text
┌────────────────────────────────────────┐
│                                        │
│         📦                             │
│                                        │
│   Заказов не найдено                   │
│                                        │
│   Попробуйте изменить фильтры или     │
│   создайте новый заказ                 │
│                                        │
│   [Сбросить фильтры]                   │
│                                        │
└────────────────────────────────────────┘
Задача 10.5: Горячие клавиши
Полезные shortcuts
Глобальные:

Ctrl + K — Открыть глобальный поиск

Esc — Закрыть модалку/dropdown

/ — Фокус в поиск

В таблице заказов:

↑↓ — Навигация по строкам

Enter — Открыть выбранный заказ

Ctrl + A — Выбрать все на странице

В детальной странице заказа:

E — Редактировать заказ

C — Создать задачу

N — Написать клиенту

Как показать:
Добавить раздел "Горячие клавиши" в меню пользователя или footer.

Задача 10.6: Responsive design
Мобильная версия (опционально, но полезно)
Если менеджер работает с планшета или телефона:

Канбан превращается в список (вертикальная прокрутка)

Таблица заказов становится карточками

Sidebar сворачивается в гамбургер-меню

Модалки занимают весь экран

💡 ГЕНИАЛЬНЫЕ ИДЕИ (если считаешь нужным):
Примеры:

Dark mode (переключатель в настройках)

Анимации переходов (плавные, не резкие)

Звуковые уведомления (опционально, можно отключить)

Персонализация (выбор цветовой схемы)

Accessibility (поддержка screen readers, клавиатурная навигация)

Подсказки для новых пользователей (tooltips, onboarding tour)

Статус "Online/Offline" для менеджеров

Индикатор несохраненных изменений

Автосохранение черновиков

🎯 ИТОГОВЫЕ ТРЕБОВАНИЯ И ИНСТРУКЦИИ
Общие принципы выполнения
1. ПРИОРИТЕТ ВЫПОЛНЕНИЯ
СТРОГО выполнять в порядке:

БЛОК 1: Интерактивный чеклист (БЛОКЕР)

БЛОК 2: Drag & Drop канбан (КРИТИЧНО)

БЛОК 3: Кнопки связи с клиентом

БЛОК 4: Редактируемая логистика

БЛОК 5: Редактируемые финансы

БЛОК 6: Экспорт в Excel

БЛОК 7: Массовые действия

БЛОК 8: Сохранение фильтров

БЛОК 9: Быстрое создание задач

БЛОК 10: UX полировка

Почему в таком порядке:
Блоки 1-2 — блокеры, без них CRM нерабочая.
Блоки 3-5 — критичный функционал.
Блоки 6-10 — улучшения UX.

2. СТАНДАРТЫ КАЧЕСТВА
Каждый блок должен:
✅ Работать полностью (не половина функционала)
✅ Быть протестирован (ручное тестирование минимум)
✅ Иметь обработку ошибок (try-catch, fallbacks)
✅ Иметь валидацию данных (на frontend и backend)
✅ Иметь обратную связь пользователю (toasts, loaders)
✅ Быть задокументирован (комментарии в коде)

НЕ делай:
❌ Половинчатые решения ("сделаю базово, доделаю потом")
❌ Hardcoded значения (вынести в константы)
❌ Игнорирование edge cases (что если список пустой? что если запрос failed?)
❌ Копипаст кода (выноси в переиспользуемые функции/компоненты)

3. ОБЯЗАТЕЛЬНОЕ ЧТЕНИЕ
Перед стартом ОБЯЗАТЕЛЬНО прочитай:

text
C:\Users\hacke\CascadeProjects\Finals1\eubike\docs\CRM_SPRINT2_REPORT.md
Этот файл содержит:

Реальные боли менеджера

Что конкретно не работает сейчас

Какие ожидания от системы

Контекст текущей реализации

Используй отчет для:

Понимания WHY каждой фичи (не просто "сделать", а "решить проблему")

Приоритизации (что реально важно)

Тестирования (проверь что проблемы из отчета решены)

4. ТЕСТИРОВАНИЕ
После каждого блока:

1. Функциональное тестирование:

Проверь что функция работает в "happy path"

Проверь edge cases (пустой список, большой список, некорректные данные)

Проверь обработку ошибок (что если сервер недоступен?)

2. UX тестирование:

Открой страницу как менеджер

Попробуй выполнить реальную задачу (например "проверить байк")

Засеки время — стало ли быстрее?

Спроси себя: "Хотел бы я работать с этим каждый день?"

3. Регрессионное тестирование:

Проверь что старый функционал не сломался

Открой Dashboard, Orders, Customers — всё ли работает?

5. ГЕНИАЛЬНЫЕ ИДЕИ
В каждом блоке есть секция "💡 ГЕНИАЛЬНЫЕ ИДЕИ".

Правило:
Если ты видишь очевидное улучшение которое:

✅ Реально помогает менеджеру

✅ Не сильно усложняет реализацию

✅ Не ломает существующий функционал

✅ Не добавляет "фичу ради фичи"

ТО ДЕЛАЙ ЭТО!

Примеры хороших идей:

Автозаполнение полей (экономит время)

Шаблоны сообщений (ускоряет коммуникацию)

Подсказки при ошибках (помогает избежать фрустрации)

Примеры плохих идей:

Сложная анимация (красиво но бесполезно)

Избыточная кастомизация (99% пользователей не используют)

Фичи которые никто не просил

6. СТИЛЬ КОДА И ДИЗАЙНА
Frontend:

Используй существующие UI компоненты из проекта

Следуй дизайн-системе BikeWerk (черный/белый, мягкие тени, округлые углы)

Все интерактивные элементы должны иметь hover состояние

Все действия должны иметь обратную связь (toast, loader)

Backend:

Используй существующую структуру endpoints

Следуй naming conventions проекта

Все endpoint должны иметь авторизацию (authenticateToken + requireManagerRole)

Все действия должны логироваться (audit_log, timeline events)

Базы данных:

Используй существующие таблицы где возможно

Если нужна новая таблица — создай миграцию

Все изменения должны быть транзакционными

Индексы на часто запрашиваемые поля

7. ДОКУМЕНТАЦИЯ
После завершения каждого блока:

Создай краткий отчет в формате:

text
## БЛОК X: [НАЗВАНИЕ]

✅ Что реализовано:
- [Список фич]

✅ Endpoints добавлены:
- [Список API]

✅ Компоненты добавлены:
- [Список файлов]

✅ Протестировано:
- [Что проверил]

⚠️ Known issues (если есть):
- [Список]

💡 Улучшения сделаны сверх ТЗ:
- [Список]
8. КРИТЕРИИ УСПЕХА
Sprint 3 считается успешным если:

✅ Все 10 блоков реализованы полностью
✅ Менеджер может выполнять ВСЮ работу в CRM (без Excel/Telegram)
✅ Чеклист 28 пунктов полностью интерактивен
✅ Канбан с Drag & Drop работает плавно
✅ Нет критичных багов
✅ Скорость работы менеджера увеличилась в 2-3 раза

Ожидаемая оценка после Sprint 3: 9-10/10

9. ВРЕМЕНИ И ОЦЕНКА СЛОЖНОСТИ
Оценка времени по блокам:

БЛОК 1 (Чеклист): 90-120 минут

БЛОК 2 (Канбан): 40-60 минут

БЛОК 3 (Контакты): 20-30 минут

БЛОК 4 (Логистика): 30-40 минут

БЛОК 5 (Финансы): 30-40 минут

БЛОК 6 (Экспорт): 20-30 минут

БЛОК 7 (Массовые действия): 30-40 минут

БЛОК 8 (Фильтры в URL): 15-20 минут

БЛОК 9 (Быстрые задачи): 20-30 минут

БЛОК 10 (UX полировка): 40-60 минут

ИТОГО: ~350-470 минут (5-8 часов реальной работы модели)

Можно разбить на 2 захода:

Заход 1: Блоки 1-5 (критичный функционал)

Заход 2: Блоки 6-10 (улучшения)

10. ФИНАЛЬНАЯ ПРОВЕРКА
Перед завершением Sprint 3 проверь:

Чеклист функций:

 Чеклист: можно кликнуть на пункт

 Чеклист: можно добавить комментарий

 Чеклист: можно загрузить фото

 Чеклист: прогресс обновляется

 Чеклист: AI оценка работает

 Канбан: карточки перетаскиваются

 Канбан: статус обновляется на сервере

 Контакты: кнопка "Позвонить" работает

 Контакты: кнопка "WhatsApp" работает

 Контакты: кнопка "Email" работает

 Логистика: можно вписать tracking

 Логистика: можно отправить клиенту

 Финансы: можно добавить транзакцию

 Финансы: история платежей отображается

 Экспорт: CSV скачивается

 Массовые действия: чекбоксы работают

 Массовые действия: панель появляется

 Массовые действия: назначение работает

 Фильтры: сохраняются в URL

 Фильтры: применяются при загрузке

 Быстрая задача: создается из заказа

 UX: все зоны кликабельны

 UX: есть loaders

 UX: есть toasts

Если хотя бы один пункт НЕ работает — Sprint 3 НЕ завершен.

🚀 НАЧАЛО РАБОТЫ
Последовательность действий
ШАГ 1: Прочитай файл C:\Users\hacke\CascadeProjects\Finals1\eubike\docs\CRM_SPRINT2_REPORT.md

ШАГ 2: Убедись что понимаешь:

Почему оценка 3/10

Какие главные проблемы

Что ожидает менеджер

ШАГ 3: Начни с БЛОКА 1 (Чеклист):

Сначала Backend (все 4 endpoint)

Потом Frontend (компоненты)

Протестируй

Сделай отчет

ШАГ 4: Переходи к БЛОКУ 2 (Канбан)

И так далее до БЛОКА 10

ШАГ 5: Финальная проверка всех функций

ШАГ 6: Составь итоговый отчет Sprint 3

Итоговый отчет (формат)
text
═══════════════════════════════════════════════════════════════════
  CRM SPRINT 3: ИТОГОВЫЙ ОТЧЕТ
  Дата: [дата]
  Время выполнения: [X] минут
═══════════════════════════════════════════════════════════════════

## EXECUTIVE SUMMARY

Все 10 критичных проблем решены. CRM превратилась из "read-only дашборда" 
в полноценный инструмент работы.

Ожидаемая оценка: 9/10

## ЧТО СДЕЛАНО

[Список всех 10 блоков с деталями]

## НОВЫЕ ENDPOINTS

[Список всех API]

## НОВЫЕ КОМПОНЕНТЫ

[Список файлов]

## ТЕСТИРОВАНИЕ

[Что протестировано, какие кейсы]

## УЛУЧШЕНИЯ СВЕРХ ТЗ

[Список гениальных идей которые внедрил]

## KNOWN ISSUES

[Если есть мелкие недочеты]

## NEXT STEPS

[Что можно улучшить в будущем]

═══════════════════════════════════════════════════════════════════
🎯 УДАЧИ!
Помни:

Ты создаешь инструмент для реального человека

Каждая фича должна решать реальную проблему

Качество важнее скорости

Если не уверен — спроси/уточни

Тестируй как будто это твоя работа

Цель: Сделать CRM которой менеджер будет РАДА пользоваться каждый день.