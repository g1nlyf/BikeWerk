# 🎨 EUBike Frontend

Клиентская часть проекта EUBike — современное Single Page Application (SPA), разработанное для обеспечения максимальной производительности, SEO-оптимизации и удобства пользователей.

---

## 🛠 Технологический стек

*   **Core:** React 19, Vite (сборщик).
*   **Стилизация:** Tailwind CSS 3.4, Shadcn UI (компоненты на базе Radix UI).
*   **Анимации:** Framer Motion.
*   **Состояние и Логика:** React Context API, Custom Hooks.
*   **Маршрутизация:** Кастомный легковесный роутер (без react-router-dom) для максимального контроля над рендерингом.

---

## 📂 Структура проекта

```text
frontend/
├── src/
│   ├── api/                # Типы и базовые настройки API
│   ├── components/         # UI компоненты
│   │   ├── blocks/         # Крупные блоки (Hero, FAQ, секции лендинга)
│   │   ├── catalog/        # Компоненты каталога (BikeCard, фильтры)
│   │   ├── checkout/       # Модалки и формы оформления заказа
│   │   ├── landing/        # Секции главной страницы (Bikeflip стиль)
│   │   ├── ui/             # Базовые атомарные компоненты (Button, Input)
│   ├── context/            # Глобальные контексты (LeadSystem, Analytics)
│   ├── lib/                # Утилиты, API клиенты, хелперы
│   ├── pages/              # Страницы приложения
│   │   ├── CatalogPage.tsx      # Основной каталог
│   │   ├── ProductDetailPage.tsx # Страница товара
│   │   ├── CalculatorPage.tsx    # Калькулятор доставки
│   │   ├── AdminDashboardPage.tsx # Админ-панель
│   ├── routes/             # Логика маршрутизации (AppRouter.tsx)
```

---

## 🚀 Основные модули

### 1. Каталог (`/catalog`)
*   **Фильтрация:** Поддерживает фильтры по бренду, дисциплине (Gravel, MTB, etc.), цене, году.
*   **Умный поиск:** Интеграция с бэкендом для поиска по ключевым словам.
*   **UI:** Адаптивная сетка, скелетоны при загрузке, пагинация.

### 2. Карточка товара (`/product/:id`)
*   **Галерея:** Интерактивный просмотр фото.
*   **Характеристики:** Детальное описание компонентов.
*   **Покупка:** Интеграция с корзиной и быстрым заказом.

### 3. Лендинг (Главная страница)
*   Высокопроизводительный лендинг с анимациями.
*   Секции: "Лучшее время доставки", "Горячие предложения", "Отзывы".
*   Адаптирован под мобильные устройства (Mobile First подход).

### 4. Калькулятор (`/calculator`)
*   Инструмент для расчета стоимости доставки велосипеда "под ключ" из Европы.
*   Учитывает вес, страну отправления и дополнительные услуги (страховка, ТО).

### 5. Оформление заказа
*   **Guest Checkout:** Оформление без регистрации.
*   **Wizard:** Пошаговый мастер оформления (Данные -> Доставка -> Оплата).

---

## 🔧 Запуск и Разработка

### Установка зависимостей
```bash
cd frontend
npm install
```

### Запуск сервера разработки
```bash
npm run dev
```
Приложение будет доступно по адресу `http://localhost:5173`.

### Сборка для продакшена
```bash
npm run build
```
Результат сборки будет в папке `dist/`.

---

## 🧩 Особенности архитектуры

*   **Custom Router (`AppRouter.tsx`):** Используется собственная реализация роутинга для упрощения бандла и специфической логики переходов (например, для модальных окон поверх контента).
*   **API Client (`lib/api-client.ts`):** Единая точка входа для всех запросов к бэкенду. Автоматически добавляет токены авторизации и обрабатывает ошибки.
*   **Компоненты UI:** Используется подход "Copy-Paste" компонентов (Shadcn UI), что позволяет полностью кастомизировать дизайн-систему под нужды проекта.

---

## 🧪 Тестирование

В проекте настроен **Playwright** для E2E тестирования критического функционала (корзина, каталог, оформление заказа).

```bash
npx playwright test
```
