# Исправление проблемы с секретами в Git

## Проблема
GitHub блокирует push из-за обнаруженного секрета (Notion API Token) в истории коммитов.

## Решение

### ✅ Файл уже исправлен
Файл `backend/src/apis/js/notion-config.js` уже исправлен - токен заменен на переменную окружения.

### 🚀 Быстрое решение (рекомендуется)

1. **Запустите скрипт:**
   ```powershell
   .\fix-git-push.ps1
   ```

2. **Или вручную:**
   - Откройте ссылку в браузере:
     ```
     https://github.com/g1nlyf/BikeWerk/security/secret-scanning/unblock-secret/39Fc2XLzlTY16PGEKxIM2bpqlrt
     ```
   - Нажмите **"Allow secret"** (секрет уже удален из кода)
   - Выполните:
     ```powershell
     git push origin main
     ```

### 🔧 Альтернативные скрипты

- **`fix-git-push.ps1`** - простой скрипт с вариантами (рекомендуется)
- **`fix-git-secrets-simple.ps1`** - простой скрипт с выбором вариантов
- **`fix-git-secrets-auto.ps1`** - полностью автоматический (переписывает историю)

### 📝 Важно: Настройка переменных окружения

Добавьте в `backend/.env` (или корневой `.env`):
```
NOTION_API_TOKEN=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
NOTION_DATABASE_ID=271972f4eb4a8004939bc6e98c699437
```

## Примечание

Секрет уже удален из кода, но остался в истории git. GitHub требует либо:
- Разрешить секрет через web interface (вариант 1 - проще)
- Переписать историю git (вариант 2 - полностью удаляет секрет)
